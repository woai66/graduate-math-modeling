# Q2 建模阶段回执

状态：PARTIAL

已对附件 2 的 10 张图像进行高斯平滑、Canny 边缘提取、坐标归一化和 DBSCAN 聚类，并对可用簇使用 RANSAC 线性化正弦模型拟合。当前共拟合 175 个候选簇，平均 R² 和 RMSE 已写入 `results/q2_cluster_sine_results.csv`。

当前结果仍是候选簇级别，存在碎片簇、负 R² 和多条裂隙交叠问题。正式表 1 还需要按图像逐裂隙筛选、合并和人工复核，并补充拟合曲线图、参数单位和失败样本。
